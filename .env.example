# AWS Bedrock — copy this file to .env.local and fill in values
# .env.local is gitignored and must NEVER be committed

# If running on EC2 with an IAM instance role that has Bedrock access,
# you can omit the key/secret and only set REGION and MODEL_ID.

# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=   # only for temporary/STS credentials

AWS_REGION=us-east-1

# Verify model ID in AWS Console → Bedrock → Model access
BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-5-20250929-v1:0
